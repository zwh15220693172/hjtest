4dff

/**
 * 1.先对字符串做判断
 * 1.1是否有非法的符号
 * 1.2是否有大写的英文字母
 * 2.在展开的时候，注意，digit可能是一位数以上比如123a
 * 2.在展开的时候，获取123a以后，还要对下一个字符做判断，比如4a4a也是不行的
 * 2.digit数字必须要大于2
 * 3.检查数组中是否有连续的cur，但是并没有压缩的
 * 错误案例
 * Aed
 * e@d
 * 2d4f
 * 4f3f
 * 4f5
 * 04d
 * ddd
 */