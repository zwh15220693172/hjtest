nums = [4, 3, 2, 3, 5, 2, 1], k = 4
1.先对nums求和
2.如果%4不等于0那么没有结果
3.%4得到target
4.创建一个数组，长度为4并且每一个值都为target
5.状态递归，每一个数都去试试，看看能不能减去nums[i]如果可以继续，直到可以为止

关于这道题可以脑残遍历法，先取数组里面的最大值，然后一直到sum / 2 + 1，而这个数为target
一直遍历是否可以分成等和子集target